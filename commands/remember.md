---
description: Store a memory about this project, a decision, preference, or learning
allowed-tools: mcp__odsp-memory__memory_remember
---

Store a memory using the OneDrive memory system.

Parse the arguments: the first word is the category (project, decision, preference, learning, task), and the rest is the content.

If no category is provided, infer the best category from the content.

Arguments: $ARGUMENTS

Use the `memory_remember` tool with the parsed category and content.
